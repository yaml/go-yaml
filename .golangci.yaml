version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
  disable:
    - unused
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
formatters:
  enable:
    - gofmt
  exclusions:
    generated: lax
