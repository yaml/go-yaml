version: '2'

linters:
  enable:
  - dupword
  - govet
  disable:
  - errcheck
  - ineffassign
  - staticcheck
  - unused
  exclusions:
    generated: lax
    presets:
    - comments
    - common-false-positives
    - legacy
    - std-error-handling
  settings:
    dupword:
      ignore:
      - 'NULL'
      - DOCUMENT-START
      - BLOCK-END

formatters:
  enable:
  - gofmt
  exclusions:
    generated: lax
